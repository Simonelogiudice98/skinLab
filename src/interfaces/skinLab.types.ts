export type Concern = {
  title: string;
  description: string;
  treatments: string[];
};

export type Treatment = {
  title: string;
  priceFrom: string;
  description: string;
  bullets: string[];
};
